package SystemDesign.basicConcepts.network;

/**
 * Will also create a session in the database with the login event and give you the sessionid in form of cookie,
 *
 * Cookie is only a medium used to transport the sessionId and it is used because it is convenient and browser will
 * always send the cookies with every request.
 */
public class CookiesAndSession {
}
